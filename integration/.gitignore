# Build trees. Every project here builds two ways - CMake for the host
# verifier, PlatformIO for the board - and neither output belongs in the
# repository: they are large, machine-specific, and regenerated by CI on
# every run.
build/
.pio/
.probe/
